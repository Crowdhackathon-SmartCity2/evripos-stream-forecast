package com.philcham.evriposstream5;

import android.app.Activity;
import android.os.Bundle;


public class InfoActivity extends Activity {
    /** Called when the activity is first created. */
 
     //  public TextView InstrGr,InstrEng;
    	  /** Called when the activity is first created. */
        @Override
		public void onCreate(Bundle savedInstanceState) {
            super.onCreate(savedInstanceState);
            setContentView(R.layout.instruction);                             
           // InstrGr=(TextView)findViewById(R.id.InsGr); 
           // InstrEng=(TextView)findViewById(R.id.InsEng); 
            
      			//	Instr.setText(new StringBuilder().append("Αυτό είναι ένα πρόγραμμα που μπορείς να αναλύσεις και να υπολογίσεις τις αποδόσεις που έχεις παίξει στο σύστημα σου."+
				              }
}

	
  